package com.kodilla.stream.beautifier;

public class PoemBeautifier {
    public static void decorate(String stringToDecorate, PoemDecorator poemDecorator) {
        poemDecorator.decorateText(stringToDecorate);
    }
}
